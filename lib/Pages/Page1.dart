import 'package:flutter/material.dart';

class PageOne extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
     return new Scaffold(
       body:new  Image(image: AssetImage('images/kk.png')) ,
     );
  }
}